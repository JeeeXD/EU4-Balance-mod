namespace = roleplay_event

country_event = {
	id = roleplay_event.1
	title = roleplay_event.1.T
	desc = roleplay_event.1.D
	picture = FEUDALISM_eventPicture

	trigger = {
		has_country_flag = cav_ratio_reward_flag
	}

	mean_time_to_happen = {
		days = 1
	}

	option = {
		name = roleplay_event.1.OPT1

		add_country_modifier = {
			name = cav_ratio_reward
			duration = 10950
		}
		clr_country_flag = cav_ratio_reward_flag
	}
}

country_event = {
	id = roleplay_event.2
	title = roleplay_event.2.T
	desc = roleplay_event.2.D
	picture = FEUDALISM_eventPicture

		trigger = {
			has_country_flag = interest_reward_flag
		}

		mean_time_to_happen = {
			days = 1
		}

	option = {
		name = roleplay_event.2.OPT1 

		add_country_modifier = {
			name = interest_reward
			duration = 10950
		}
		clr_country_flag = interest_reward_flag
	}
}

country_event = {
	id = roleplay_event.3
	title = roleplay_event.3.T
	desc = roleplay_event.3.D
	picture = FEUDALISM_eventPicture

	trigger = {
		has_country_flag = naval_reward_flag
	}

	mean_time_to_happen = {
		days = 1
	}

	option = {
		name = roleplay_event.3.OPT1

		add_country_modifier = {
			name = naval_reward
			duration = 10950
		}

		clr_country_flag = naval_reward_flag
	}
}

country_event = {
	id = roleplay_event.4
	title = roleplay_event.4.T
	desc = roleplay_event.4.D
	picture = FEUDALISM_eventPicture

	trigger = {
		has_country_flag = colonist_reward_flag
	}

	mean_time_to_happen = {
		days = 1
	}

	option = {
		name = roleplay_event.4.OPT1

		add_country_modifier = {
			name = colonist_reward
			duration = 5475
		}

		clr_country_flag = colonist_reward_flag
	}
}
