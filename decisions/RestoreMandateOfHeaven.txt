country_decisions = {

	restore_mandate = {
		major = yes
		potential = {
			OR = {
				tag = QNG
				tag = MNG
				tag = KOR
				tag = JAP
				tag = SIA
				tag = BRM
				tag = LFA
				has_country_flag = can_restore_mandate
			}
			NOT = {
				has_global_flag = mandate_created
			}
		}
		provinces_to_highlight = {
			
		}
		allow = {
			num_of_owned_provinces_with = {
				value = 75
				is_core = ROOT
				superregion = china_superregion
			}
			is_at_war = no
			is_free_or_tributary_trigger = yes
			is_nomad = no
		}
		effect = {
			set_emperor_of_china = ROOT
			add_prestige = 100
			add_meritocracy = 100
			set_global_flag = mandate_created
		}
		ai_will_do = {
			factor = 1
		}
		ai_importance = 400
	}
	
}
