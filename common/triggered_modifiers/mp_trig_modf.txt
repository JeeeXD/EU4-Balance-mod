CLI_free_city = {
	potential = {
		NOT = {
			tag = CLI
		}
		CLI = {
			has_country_flag = opm_cilli
		}
	}
	trigger = {
		is_emperor = yes
		CLI = {
			OR = {
				is_elector = yes
				has_reform = free_city
			}
		}
	}

	imperial_authority_value = 0.1
	
}

