CLI_free_city = {
	potential = {
		NOT = {
			tag = CLI
		}
		CLI = {
			has_country_flag = opm_cilli
		}
	}
	trigger = {
		is_emperor = yes
		CLI = {
			OR = {
				is_elector = yes
				has_reform = free_city
			}
		}
	}

	imperial_authority_value = 0.1
	
}

CLI_march = {
	potential = {
		is_emperor = yes
		CLI = {
			has_country_flag = opm_cilli
		}
	}
	trigger = {
		is_emperor = yes
		CLI = {
			is_subject_of_type_with_overlord = {
			who = emperor
			type = march
		    }
		}
		
	}

	imperial_authority_value = 0.1
	
}

CLI_dead = {
	potential = {
		is_emperor = yes
		emperor = {
			ai = no
		}
		has_global_flag = opm_cilli_global
	}
	trigger = {
		NOT = {
			exists = CLI
		}
	}

	reasons_to_elect = -50
	imperial_authority = -0.25
}

CLI_local = {
	potential = {
		tag = CLI
		has_country_flag = opm_cilli
	}

	trigger = {
		tag = CLI
		OR = {
			has_reform = free_city
			is_elector = yes
		}
	    
	}

	global_tax_income = 24
	development_cost = -0.1
	global_allowed_num_of_buildings = 1
	global_allowed_num_of_manufactories = 1

}

CLI_toobig = {
	potential = {
		tag = CLI
		has_country_flag = opm_cilli
	}
	trigger = {
		tag = CLI
		num_of_cities = 2
	    
	}

	administrative_efficiency = -1

}
